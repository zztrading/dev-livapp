/**
 * V8 Playground pass threshold.
 * Score must be >= PASS_SCORE to count as "passed".
 * This means the user needs >80 (i.e. 81+).
 */
export const PASS_SCORE = 81;

/**
 * Minimum score to unlock insight XP rewards.
 * Score must be >= INSIGHT_UNLOCK_SCORE to claim XP.
 */
export const INSIGHT_UNLOCK_SCORE = 70;
